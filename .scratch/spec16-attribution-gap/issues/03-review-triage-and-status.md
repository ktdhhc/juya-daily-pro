# 03: 审核台呈现与处置（分组 + 候选状态端点）

**What to build:** 审核台把"多家命中且有待处置候选"的条目收进「缺候选待入册」，候选区展示合并后的证据；「标记已入册 / 忽略」两个按钮可用并落库。验收者能在 /review 看到缺主角的条目、看到某家公司被反复提到的证据，并把不打算入册的候选一键忽略。

**Blocked by:** 02（候选落库）

**Status:** done（验收证据见 ../review.md「验收证据」）

- [x] 待办分组：多家命中且存在待处置候选的条目归入「缺候选待入册」（纯函数测试，含零命中条目仍归此 tab 的既有行为）
- [x] 候选状态端点：管理员可把候选标记为已入册或忽略；非法 status / 不存在的 id → 400；未授权 → 403
- [x] 候选区展示合并后的证据文本，两个按钮可用，成功后候选从列表消失
- [x] 浏览器走查（手机 + 桌面各一遍）：点击「忽略」→ 候选消失且状态落库为已忽略；点击「标记已入册」→ 状态落库为已入册
- [x] 既有测试套件零回归（typecheck 通过）
