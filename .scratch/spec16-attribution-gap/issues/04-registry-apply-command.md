# 04: 入册闭环命令（一条命令跑完）

**What to build:** 一条命令完成"重新生成 registry → 刷新 D1 公司镜像并重匹配 → 全量重匹配 → 候选状态回填"：编辑把候选写进 `data/companies.yaml` 后跑一次，对应的待处置候选自动标记为已入册、相关条目重新匹配上新公司。命令**不触发解析**（决策：改判主导交给下一轮定时任务或手动运行解析）。

**Blocked by:** 02（候选落库）

**Status:** done（验收证据见 ../review.md「验收证据」）

- [x] 纯函数判定：给定新 registry 的公司 id 集合与待处置候选清单 → 返回应标记为已入册的候选 id（只认 id 精确命中；不在 registry 的保持待处置）
- [x] 命令按序执行四步（重新生成 registry → 刷新镜像并窗口重匹配 → 全量重匹配 → 候选状态回填），任一步失败即止并给出可读错误
- [x] 本地实跑演练：往 `data/companies.yaml` 加一家公司 → 跑命令 → 对应候选状态变已入册、原本缺归属的条目挂上该公司
- [x] 命令结束时打印提示：下一轮定时任务或手动「运行解析」会经已发布补救通道补判主次
- [x] 既有测试套件零回归（typecheck 通过）
